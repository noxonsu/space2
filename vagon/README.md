# 🚢 Админка портового терминала

Система для анализа данных портового терминала с использованием ИИ для генерации SQL запросов. Приложение построено на Flask.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка Python пакетов
pip install -r requirements.txt
```

*Примечание: Для установки `pyodbc` в Linux может потребоваться установить системные зависимости, например `unixodbc-dev`.*
```bash
# Пример для Debian/Ubuntu
sudo apt-get install unixodbc-dev
```

### 2. Установка ODBC-драйвера для SQL Server (Обязательно)

Для подключения к MS SQL Server из Linux-окружения (например, из Codespaces) необходимо установить официальный ODBC-драйвер от Microsoft.

**Для систем на базе Debian/Ubuntu:**
Выполните следующие команды в терминале, чтобы зарегистрировать ключ Microsoft, добавить репозиторий и установить драйвер:
```bash
sudo apt-get update
sudo apt-get install -y curl apt-transport-https
curl https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add -
curl https://packages.microsoft.com/config/debian/11/prod.list | sudo tee /etc/apt/sources.list.d/mssql-release.list
sudo apt-get update
sudo ACCEPT_EULA=Y apt-get install -y msodbcsql17
```

### 3. Настройка базы данных и токена HuggingFace

Убедитесь, что файл `.env` содержит правильные данные для подключения:

```env
DB_HOST=

```

### 4. Получение токена HuggingFace

1. Зарегистрируйтесь на [HuggingFace](https://huggingface.co/) и создайте аккаунт.
2. Перейдите в настройки профиля -> [Access Tokens](https://huggingface.co/settings/tokens).
3. Создайте новый токен с правами "Read".
4. Скопируйте токен и вставьте его в файл `.env`.

### 5. Запуск админки

```bash
# Запуск Flask-приложения
pip install -r requirements.txt
```

Админка будет доступна по адресу: http://localhost:8080

### Запуск в Codespaces

Приложение автоматически определит, что запущено в Codespaces, и настроит URL для API.

**1. Запустите Flask бэкенд в первом терминале:**
```bash
python vagon/app.py
```

**2. Запустите Streamlit фронтенд во втором терминале:**
```bash
streamlit run vagon/vagon_dashboard.py
```
Codespaces автоматически перенаправит порты, и вы сможете открыть оба приложения в браузере.

## 📊 Возможности

### Основные функции:
- **Генерация SQL запросов** - ИИ автоматически создает SQL запросы на основе естественного языка.
- **Выполнение запросов** - Прямое выполнение SQL запросов к базе данных.
- **Построение графиков** - Автоматическое создание графиков по данным запросов (линейные, столбчатые, круговые).
- **Просмотр статистики** - Отображение основной статистики по таблицам базы данных.
- **Интеллектуальный анализ** - Система автоматически определяет тип графика на основе данных и запроса.

### Примеры запросов для LLM:

**Базовые запросы:**
- *Покажи 5 самых тяжелых грузов, принятых в этом месяце.*
- *Сколько вагонов было выгружено вчера по каждому типу груза?*
- *Список всех судов, которые были на причале в июне.*

**Запросы с построением графиков:**
- *Сделай график выгрузки вагонов по месяцам за последний год.*
- *Покажи динамику погрузки судов помесячно.*
- *Построй график распределения грузов по типам.*
- *Сделай график количества вагонов на предприятии по месяцам.*

##  Файлы проекта

### Основные файлы:
- **`app.py`** - Главный файл Flask-приложения (бэкенд API).
- **`templates/index.html`** - HTML-разметка и JS-логика фронтенда.
- **`requirements.txt`** - Python зависимости включая matplotlib для графиков.
- **`.env`** - Файл с настройками подключения и токенами.
- **`db_shema.sql`** - Схема базы данных (только для справки).

### Компоненты системы:
- **`DatabaseManager`** - Управление подключениями к базе данных и выполнение запросов.
- **`LLMQueryGenerator`** - Генерация SQL запросов через HuggingFace API.
- **`ChartGenerator`** - Создание графиков с помощью matplotlib.

### Устаревшие файлы:
- **`admin_dashboard.py`** - Старая версия на Streamlit.
- **`vagon_dashboard.py`** - Старая версия на Streamlit.

### API Endpoints:
- **`/api/generate-sql`** - Генерация SQL запроса из текста.
- **`/api/execute-sql`** - Выполнение SQL запроса.
- **`/api/execute-sql-with-chart`** - Выполнение SQL + создание графика.
- **`/api/get-schema`** - Получение схемы базы данных.
- **`/api/stats`** - Статистика по таблицам.
- **`/api/models`** - Управление LLM моделями.

## 🤖 Использование ИИ и построение графиков

### Модели LLM
Система использует **Qwen/Qwen2.5-Coder-7B** через HuggingFace API для генерации SQL запросов из естественного языка.

**Доступные модели:**
- `Qwen/Qwen3-4B-fast` (по умолчанию)
- `Qwen/Qwen2.5-Coder-7B`
- `Qwen/Qwen2.5-Coder-0.5B-Instruct`
- `deepseek-ai/DeepSeek-Coder-V2-Lite-Instruct`
- `codellama/CodeLlama-7b-Instruct-hf`
- `meta-llama/Meta-Llama-3-8B-Instruct`

### Построение графиков
Система автоматически создает графики с помощью **matplotlib**:

**Типы графиков:**
- **Линейный график** - для временных рядов и динамики
- **Столбчатый график** - для сравнения значений
- **Круговая диаграмма** - для распределения по категориям

**Автоматическое определение типа:**
- Запросы с "динамика", "тренд", "по месяцам" → линейный график
- Запросы с "сравни", "топ", "больше" → столбчатый график  
- Запросы с "распределение", "доля", "процент" → круговая диаграмма

### Пример работы с графиками

1. **Запрос на естественном языке**: "Сделай график выгрузки вагонов по месяцам"
2. **Сгенерированный SQL**: `SELECT DATEPART(year, [Дата и время выгрузки]) AS Year, DATEPART(month, [Дата и время выгрузки]) AS Month, COUNT(*) AS Count FROM [GRNG/GRMU].[dbo].[VagonUnloading] GROUP BY DATEPART(year, [Дата и время выгрузки]), DATEPART(month, [Дата и время выгрузки]) ORDER BY Year, Month`
3. **Результат**: Данные + base64 строка с графиком PNG

## 🧪 Тестирование

### Запуск автотестов

Проект содержит comprehensive набор юнит и интеграционных тестов.

```bash
# Запуск всех тестов
python -m pytest tests/ -v

# Запуск тестов с подробным выводом ошибок
python -m pytest tests/ -v --tb=short

# Запуск только unit тестов
python -m pytest tests/test_services.py -v

# Запуск только интеграционных тестов
python -m pytest tests/test_integration.py -v
```

### Структура тестов

- **`tests/test_services.py`** - Unit тесты для отдельных компонентов:
  - `DatabaseManager` - тесты подключения к БД и выполнения запросов
  - `LLMQueryGenerator` - тесты генерации SQL через HuggingFace API

- **`tests/test_integration.py`** - Интеграционные тесты:
  - API endpoints (`/api/stats`, `/api/generate-sql`, `/api/execute-sql`)
  - Полный цикл: генерация SQL → выполнение → получение результата
  - Обработка ошибок и валидация данных

- **`tests/test_charts.py`** - Тесты построения графиков:
  - `ChartGenerator` - создание всех типов графиков
  - Валидация base64 строк графиков
  - Обработка edge cases (пустые данные, некорректные типы)

### Покрытие тестирования

✅ Подключение к базе данных  
✅ Выполнение SQL запросов  
✅ Генерация SQL через LLM  
✅ API endpoints  
✅ Построение графиков  
✅ Обработка ошибок  
✅ Валидация данных  

**Статистика тестов:** 25+ тестов, все проходят успешно

## 🛠️ Решение проблем и лучшие практики

### Частые проблемы и их решение

**1. Ошибки SQL Server GROUP BY**
```
Ошибка: "Column 'X' is invalid in the select list because it is not contained in either an aggregate function or the GROUP BY clause"
```
**Решение**: LLM настроен на правильное использование GROUP BY. Для временных рядов используется `DATEPART(year, column), DATEPART(month, column)`.

**2. Превышение лимита токенов LLM**
```
Ошибка: "Token limit exceeded"
```
**Решение**: Увеличен лимит до 3000 токенов. Если проблема остается, используйте более компактные запросы.

**3. Проблемы с подключением к базе данных**
```
Ошибка: "No suitable driver found"
```
**Решение**: Убедитесь, что установлен ODBC драйвер для SQL Server (см. секцию установки).

**4. Проблемы с графиками**
```
Ошибка: "Chart generation failed"
```
**Решение**: Проверьте, что данные содержат минимум 2 столбца. Система автоматически обрабатывает пустые результаты.

### Лучшие практики для запросов

**Для получения временных рядов:**
- ❌ "Покажи данные по месяцам" (слишком общий)
- ✅ "Сделай график выгрузки вагонов по месяцам за последний год"

**Для категорийного анализа:**
- ❌ "Покажи грузы" (слишком общий)
- ✅ "Покажи распределение грузов по типам за последний месяц"

**Для сравнения:**
- ❌ "Сравни" (неясно что сравнивать)
- ✅ "Сравни объемы выгрузки по грузоотправителям"

### Оптимизация производительности

- **Ограничивайте временные диапазоны**: добавляйте "за последний месяц", "за этот год"
- **Используйте TOP N**: для больших таблиц указывайте "топ 10", "первые 100"
- **Группируйте данные**: для графиков используйте агрегацию по дням/месяцам

### Безопасность

- **Схема базы данных жестко задана** в коде - LLM не может обратиться к несуществующим таблицам
- **Валидация SQL запросов** - проверка на использование только разрешенных таблиц
- **Токен HuggingFace** - используйте токен только с правами "Read"

## 📈 Производительность

### Метрики системы
- **Время генерации SQL**: ~2-5 секунд
- **Время выполнения запроса**: зависит от сложности (~0.5-10 секунд)
- **Время создания графика**: ~1-2 секунды
- **Размер графика**: ~20-50KB (base64 PNG)

### Рекомендации по масштабированию
- Для больших объемов данных используйте индексы на временных полях
- Кэшируйте часто используемые запросы
- Используйте пагинацию для больших результатов

## 🔄 Обновления и поддержка

### Версионирование
- **v1.0** - Базовая функциональность генерации SQL
- **v1.1** - Добавлена поддержка графиков
- **v1.2** - Жестко заданная схема БД, улучшенная валидация

### Планы развития
- [ ] Добавление кэширования запросов
- [ ] Поддержка экспорта графиков в различные форматы
- [ ] Интеграция с дополнительными LLM моделями
- [ ] Добавление dashboard с готовыми аналитическими панелями

## 💬 Telegram Бот

### 1. Получение токена Telegram Бота

1. Откройте Telegram и найдите @BotFather.
2. Отправьте команду `/newbot` и следуйте инструкциям, чтобы создать нового бота.
3. BotFather выдаст вам токен. Скопируйте его.

### 2. Настройка токена

Добавьте полученный токен в файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_БОТА
```

### 3. Запуск Telegram Бота

Убедитесь, что Flask бэкенд (`app.py`) запущен, так как Telegram бот взаимодействует с ним через API.

```bash
# Запуск Telegram бота
python vagon/telegram_bot.py
```

После запуска бот будет ожидать ваших сообщений в Telegram.
